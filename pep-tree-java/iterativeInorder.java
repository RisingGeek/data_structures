import java.util.LinkedList;

public class iterativeInorder {
    public class NodeClass {
        Node node = null;
        boolean p = false;
        boolean l = false;
        boolean r = false;
        NodeClass(Node node) {
            
        }
    }
    public static void itIn(Node root) {
        LinkedList<Node>stack = new LinkedList<>();
        stack.addFirst(root);

        while(stack.size() > 0) {
            if()
        }
    }
}